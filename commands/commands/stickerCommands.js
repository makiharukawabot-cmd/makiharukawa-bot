module.exports = async (sock, msg, from, body, parts, cmd) => {
  switch (cmd) {
    case '#delpack':
      // Logic to delete pack
      break;

    // Implement all sticker commands from menu
  }
};
